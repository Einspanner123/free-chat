[project]
name = "freechat-protocol"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["grpcio>=1.74,<2", "protobuf>=6.31,<7"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/freechat"]
