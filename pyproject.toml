[project]
name = "freechat-infra-workspace"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["libs/*", "services/*", "worker"]

[dependency-groups]
dev = [
  "hypothesis>=6.138,<7",
  "httpx2>=2.9,<3",
  "langgraph==1.2.11",
  "grpcio-tools>=1.74,<2",
  "mypy>=1.17,<2",
  "openai-agents==0.22.0",
  "pytest>=8.4,<10",
  "pytest-asyncio>=1.1,<2",
  "pytest-cov>=6.2,<8",
  "respx>=0.22,<1",
  "ruff>=0.12,<1",
  "types-psutil>=7.0,<8",
  "types-grpcio>=1.0,<2",
]

[tool.pytest.ini_options]
testpaths = ["tests", "libs", "services", "worker"]
pythonpath = ["."]
addopts = "-ra --strict-markers"
asyncio_mode = "auto"
markers = [
  "gpu: requires a real CUDA GPU and pinned worker image",
  "multinode: requires ross and workstation",
  "scale: synthetic control-plane scale validation",
]

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [
  "libs/protocol/src/freechat/control/v1/*_pb2*.py",
  "libs/protocol/src/freechat/control/v1/*_pb2*.pyi",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_configs = true
plugins = ["pydantic.mypy"]
exclude = "^libs/protocol/src/freechat/control/v1/control_pb2(_grpc)?\\.(py|pyi)$"
