[project]
name = "rlhf"
version = "0.1.0"
description = "RLHF (PPO) Pipeline for LLM Alignment"
requires-python = ">=3.12"
dependencies = ["loguru>=0.7.3"]
dev = ["pytest>=8.0"]
[tool.pytest.ini_options]
testpaths = ["tests"]
