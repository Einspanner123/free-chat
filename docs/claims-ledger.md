# Claims ledger

Only entries marked `VERIFIED` may appear as performance or production claims
in project-facing material.

| Claim ID | Status | Claim | Evidence |
|---|---|---|---|
| completed-prefix-engine-control | VERIFIED | Engine-owner lifecycle updates operate only on matching completed-request KV residency; duplicates do not extend protection and recycled blocks are not protected under stale lineage. | 2026-09-21 local date, real Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, 1024 context/128 blocks, PyTorch 2.13.0+cu130, A5000 and A4000. Each final run protects 16 resident blocks across six distinct 896-token pressure requests, clears protection on Resume, then reports zero resident/protected blocks after another six pressure requests. Cache reset discards old lineage; in-flight updates reject, and an explicitly aborted request accepts cancellation with zero protected blocks. Wrong tenant, same-sequence conflict and reopening after Resume reject; duplicate receipt is unchanged except replay marker. Repeated target inference reports 240 cached tokens with identical generated tokens; no baseline comparison or performance gain is implied. Fork `39b61e225dce4026242d2d115089734b3c092928`; development image `sha256:f923e060e328ced0492f46d8d0acfe5eb7590750902e2ec33315c959e502fb9d`, with all three patched engine files SHA-256 matched to source. Stdout and normal `freechat-prefix-pinned-a5000` / `freechat-prefix-pinned-a4000` logs, both exit 0. Fork unit tests: 28 pass via isolated pure-module import (not a GPU substitute). Public Gateway/Scheduler/Harness transport, durable command replay, shared-owner policy arbitration, offload and end-to-end benefits remain unverified. |
| sdk-managed-gpu-request-closure | VERIFIED | OpenAI Agents SDK function-tool execution produces two managed GPU requests per run, preserves Active/Resume request hints and receives confirmed releases on A5000/A4000. This verifies request execution, not task quality or standalone Tool Wait delivery. | 2026-09-21 local date, SDK 0.22.0, real Qwen2.5-0.5B BF16/eager TP=PP=DP=1, context 1024, 128 blocks, Hermes auto-tool parser. One README call and two model requests per GPU. A5000 decisions `31801e4f-0b55-4364-a504-0d2cef9b482d` / `7b9ca4cb-dbba-429e-9bb1-0b58fcb86c1f`, generation 1789944513325821948; A4000 decisions `6fed0e0a-7b0d-44e4-bb28-214d98a43591` / `0e078da4-789a-4000-a703-4e8479b3b507`, generation 1789944668181373195. Each real etcd ledger confirms 2/2 completed/released, zero outstanding. Both normal-log audits pass 2/2 releases, peak 8060928 bytes below the actual 24969216-byte pool. Both clients return exit 1 because the model adds an explanatory sentence to the required exact heading; do not count as accepted tasks. Worker image `sha256:c7eb7dffd3fc09e8c1876922b4bdadc9f3f0d2cfd146b6dd7d50f6efbd9b7e72`, control `sha256:01304005813c5e453d7860fbe1492094e547c72aeae625550ed1529de6e447b4`, Harness image `sha256:785f5d5949af15222498500bfee2593f2d6765237062d998f2e84c593816566b`. Stdout and normal `freechat-sdk-*` / `freechat-sdk4000-*` logs/state; no result directory. SDK cloud tracing disabled. No latency/throughput improvement, retention/offload, real Harness cancellation or success-rate claim. |
| historical-receipt-after-replacement | VERIFIED | An explicitly configured sealed-incarnation receipt endpoint reclaims old capacity after a replacement has registered and served GPU work, without replacing current registry ownership. | Real A4000 Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, context 1024/128 blocks and usable pool 24969216 bytes, 2026-09-21 local date. First-token SIGKILL: decision `9344ca7d-2d62-4e35-bd87-1971e86f125e`, generation 1789943278174198929, engine `398bb430-640e-469f-b35d-3807bce5c1cc`. Docker stop is proved and journal sealed before restart. Replacement generation 1789943333191986422 / engine `599ea390-7c6c-4026-967d-4e6b64a3c681` completes `7661340e-3089-4984-bb78-c25cc372db04` while old 10420224-byte reservation remains held. Independent 50054 receipt service uses no Docker socket; managed Scheduler restart with exact historical route releases the old request, then unchanged replacement completes `7bffad07-ebe1-45ab-ab2f-085465e86ad3`. Per-generation normal-log audits pass 2/2 old and 2/2 replacement; the replacement first admits work before old release, and reconstructed combined peak is 11010048 bytes below the actual pool. Four routes/releases, zero outstanding. Worker/receipt image `sha256:c7eb7dffd3fc09e8c1876922b4bdadc9f3f0d2cfd146b6dd7d50f6efbd9b7e72`; control `sha256:01304005813c5e453d7860fbe1492094e547c72aeae625550ed1529de6e447b4`. Stdout and normal `freechat-history-*` logs/state. CPU tests cover exact identity, endpoint collisions, terminal/response fences and registry preservation. Operator-assisted, same-host single-node etcd only; not automatic recovery, task resumption, HA, success-rate or RTO. |
| docker-retirement-assisted-recovery | VERIFIED | Operator-triggered stopped-container retirement closes old admission and supplies authenticated old-incarnation receipts; Scheduler reclaims the interrupted reservation before a replacement starts. The original stream fails; no task-resumption claim. | Real A5000 Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, context 1024/128 blocks, usable pool 24969216 bytes, 2026-09-21 local date. First-token SIGKILL for decision `372a9ead-b357-4d32-937d-6a29ed95ea81`, generation 1789941883334454497, engine `9b3cd309-49b5-4c16-aa13-8e69d4da25e9`. Worker image `sha256:8a9a59bea256eca305c17f82fcbe3bb1c9bca819ac064eb14dbd41e9c4fb0e18`, control `sha256:fe8a734317c9c94bf153c0218d7459bff47339c464abfe643ca2647a1a4fa6f0`, final receipt service `sha256:ac01e5588eb1913e2825d0806d0a8840afd0d482a331e91c37986af57e76a081`. Docker container `0d80bfebc616bf39667ca625fb20bcda2cbe45442a43050c0c996d5d62f4411a` is exited/PID 0; exact state mount and unique runtime ID are verified under owner locks. Sealed journal rejects late admission/live reopen and preserves observation sequence across observer reopen. Scheduler receives aborted/quiescent/admission-closed sequence 3 and releases 10420224 bytes. Receipt service SIGTERM exits 0; replacement generation 1789942360682365535 completes new decision `364977b1-83a5-4818-93d0-fa275055f56b`; three routes/releases, zero outstanding. Generation-partitioned normal-log audits pass 2/2 old and 1/1 replacement, with peaks 10420224/589824 bytes; all old releases precede replacement admission. Stdout and normal `freechat-sealed-*` logs/state. Failed hostname-based identity probes remain in `freechat-retire-*` and `freechat-sealed-observer` logs; shared-network hostname was not accepted as identity. No automatic recovery, already-replaced-registry recovery, HA, task success-rate or RTO claim. |
| worker-restart-incarnation-fence | VERIFIED | A replacement Worker rejects old execution commands; interrupted capacity is retained rather than falsely released. This is a safety boundary, not crash recovery. | A5000 real Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, context 1024/128 blocks, 2026-09-21 local date. Worker image `sha256:066466ef743541af12c24af1ce47d3e8a1ced18bc32a7c4318c6b23edf9f44e9`, control `sha256:2c9b57df30e3279733cb5212f481eed0484e195e85bdc0ccd93ac32ba959eb80`; real single-node etcd. First-token Worker SIGKILL for decision `b1c84bfb-0112-4de4-8771-d2b4896d0d8e` interrupts client with RemoteProtocolError and no DONE. Generation changes from 1789940108412062618 to 1789940234793695927; new decision `73314ce5-6620-49ba-807e-411bdcc98304` completes/releases. Old command receives FAILED_PRECONDITION execution_runtime_incarnation_mismatch; old reservation remains cancel_requested with 10420224 bytes held. Stdout and normal `freechat-crash-*` logs/state volumes. No task recovery, automatic capacity recovery, success-rate or RTO claim. |
| worker-crash-capacity-recovery | UNVERIFIED | Automatic termination capture, historical endpoint discovery/configuration and recovery coordination remain incomplete. Operator-assisted recovery both before and after replacement registration is covered separately above. | The real crash case above demonstrates the current gap: a replacement does not provide termination proof for the previous engine. Old reservation stays held. Control image `sha256:fe8a734317c9c94bf153c0218d7459bff47339c464abfe643ca2647a1a4fa6f0` reloads that real etcd record and logs `worker_generation_changed`, preserving its state/bytes; normal `freechat-crash-diagnostic-scheduler` logs, no new GPU workload. CPU regression covers registry/engine/endpoint failures, repeated reconciliation and sanitized errors. New registration, heartbeat timeout or negative query must not bypass the fence. |
| scheduler-stop-with-bus-unavailable | VERIFIED | Scheduler exits cleanly when NATS is unavailable/reconnecting, while reporting that drain did not confirm delivery. | Real container `freechat-nats-close-check`, control image `sha256:2c9b57df30e3279733cb5212f481eed0484e195e85bdc0ccd93ac32ba959eb80`, 2026-09-21 local date: after ready, NATS SIGKILL triggers connection error; SIGTERM then yields lifecycle_bus_drain_unavailable, scheduler_shutdown_complete and exit 0. CPU checks additionally cover connected, closed, reconnecting and drain timeout branches. This is process cleanup only, not pending-event delivery, in-flight task recovery or GPU performance evidence. |
| sustained-jetstream-reconnect | VERIFIED | After a 150-second NATS outage, the same Scheduler process resumes durable delivery and new GPU admission without operator restart. Initial bus setup remains deadline-bounded. | A5000 real Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, context 1024, usable KV 24969216 bytes; Worker image `sha256:066466ef743541af12c24af1ce47d3e8a1ced18bc32a7c4318c6b23edf9f44e9`, control `sha256:e22fbb438e4efc0931627ba511d7ff2666f487c9a77fa63c15926b13f1266ad9`, Compose etcd/NATS digests. Default-client baseline on control `sha256:55c0605d0ace14e8a8861ac181bfe959024c7aca314fcc96dc72684ec24c6bc7` closed after 122.46 seconds and still failed with ConnectionClosedError after NATS returned, retaining its event. Corrected runtime reconnect configuration tested with actual outage 150.46 seconds: decision `694dd5bb-f398-41be-96e3-125e97272016` generates 800 tokens, three pending events survive; afterward four logical events are readable, both outboxes empty, new decision `9f16c8b3-ef7a-467e-b364-0e02fae793c7` completes. Scheduler PID/StartedAt unchanged. Three current-generation routes/releases, no outstanding reservation. Stdout plus normal `freechat-long-nats-scheduler/gateway`, NATS/etcd/Worker logs and data volumes, 2026-09-21 local date. A separate network-isolated container fails startup with TimeoutError/exit 1 and no ready log under configured 10-second setup deadline; CPU tests cover cleanup on timeout, cancellation and stream-setup error. Fault duration is not RTO; one 150-second case does not prove arbitrary outages, durable consumer effects, HA or exactly-once. |
| gpu-jetstream-outage-replay | VERIFIED | A real GPU request completes while NATS is unavailable; execution confirmation releases capacity independently while lifecycle events remain durable until JetStream acknowledges delivery after recovery. | A5000 Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, 1024 context/128 blocks, physical usable KV pool 24969216 bytes. Two first-text-token NATS SIGKILL probes each generate 800 tokens. Final probe decision `be31e3c9-77e3-42df-b582-772bb8a158d2`: route.decided, lease.completion_pending and lease.released remain in RequestLedger.pending with publisher outbox records during outage; restart preserves original IDs, all three reach JetStream and both pending layers clear. Four total routes/releases across two warmup/probe pairs; broker-event audit peak 10420224 bytes, zero outstanding. Worker `sha256:066466ef743541af12c24af1ce47d3e8a1ced18bc32a7c4318c6b23edf9f44e9`; first-probe control `sha256:eb79502af4cc1658659ee4d2f16c1327b329a421a20969e0365dc42e9881212b`, final control `sha256:55c0605d0ace14e8a8861ac181bfe959024c7aca314fcc96dc72684ec24c6bc7`; etcd/NATS at Compose digests. Stdout and normal `freechat-verified-nats-scheduler/gateway`, `freechat-lifecycle-nats`, `freechat-recovery-a5000/etcd` logs/data volumes, 2026-09-21 local date. Short single-node outage only; no long-outage, quorum, consumer-side effects, exactly-once, RTO or success-rate claim. |
| jetstream-ack-boundaries | VERIFIED | Within the configured duplicate window, a lost publish acknowledgment retries one stored message; an unacknowledged consumer delivery is redelivered with the same stream sequence. Conflicting pending event content is rejected without removing the original. | Real etcd/NATS on final control image above, event `df5bc9ea-c719-4b87-babc-35b6d628736a`: publisher raises after actual server acknowledgment, persisted record remains; changed payload is rejected and record unchanged; retry with a later envelope timestamp publishes the original durable bytes. Pull consumer receives the same sequence twice, delivery count 2, then ack_sync clears pending ack. Unique temporary consumer removed after verification; event remains in normal JetStream storage. Five CPU cases additionally cover timestamp reconstruction and payload/tenant/generation/harness conflicts. Deduplication is bounded to configured 120 seconds; this is not permanent or application-level exactly-once. |
| scheduler-etcd-live-worker-recovery | VERIFIED | After Scheduler SIGKILL during real GPU streaming, etcd retains request ownership; restart reconciles the unchanged live Worker and only releases on a fenced terminal receipt. | workstation A5000, real Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, context 1024, 128-block pool with usable 24969216 bytes; Worker `sha256:066466ef743541af12c24af1ce47d3e8a1ced18bc32a7c4318c6b23edf9f44e9`, control `sha256:eb79502af4cc1658659ee4d2f16c1327b329a421a20969e0365dc42e9881212b`, etcd v3.6.5 at Compose digest. First-text-token fault injection for decision `d9e05d96-1eae-4f26-ab79-846eb836fcf8`, Worker generation `1789937341473574980`, engine `fcc369dc-35be-40b5-b3f0-e0ee3f98186f`; Scheduler exits 137 by intentional SIGKILL. GPU completes 800 tokens while etcd still holds active reservation 10420224 bytes. Restart obtains completed/quiescent/admission-closed receipt for that same identity, admits a new request, and drains outbox. Three total routes/releases including warmup/re-admission, zero outstanding, peak reservation 11010048 bytes; normal `freechat-recovery-scheduler/gateway/a5000/etcd` logs and stdout, 2026-09-21 local date. Single case, unchanged Worker, single-node etcd, log fallback; no Worker-crash recovery, HA, distributed exactly-once, success-rate or RTO claim. |
| etcd-write-result-contract | VERIFIED | Store writes return their own committed value/revision and concurrent stale CAS is rejected; absent-key delete returns false. | Real pinned etcd v3.6.5 via current control image: 16 concurrent CAS writers on one revision produce one winner, returned KeyValue equals stored winner, fenced delete succeeds then absent delete is false. Separate CPU regression forces another writer between write and hypothetical readback; no readback occurs. Stdout only; unique validation key removed after check. This is store-client semantics, not durable application recovery or quorum evidence. |
| scheduler-container-shutdown | VERIFIED | Idle default Scheduler handles SIGTERM/SIGINT as container PID 1 and completes cleanup with exit code 0. | Two real workstation containers, image `sha256:8143484f36e13f0022132c1557870cc57cd48bd97d40d3366d9806b43cf57976`, 2026-09-21 local date; `freechat-shutdown-sigterm/sigint` service logs contain stop_requested and shutdown_complete; stdout reports exit codes. Subprocess regression tests cover both signals. This is a control-process shutdown check only, not GPU execution, in-flight draining, dependency-fault handling, persisted restart or RTO evidence. |
| shared-scheduler-two-gpu | VERIFIED | One Scheduler routes real concurrent inference to distinct A5000/A4000 Workers, enforces each measured KV pool independently, and excludes a gracefully stopped idle Worker from new placement. | Qwen2.5-0.5B BF16/eager, TP=PP=DP=1 per Worker, context 1024, actual 128-block allocation; usable 24969216 bytes per GPU. Three rounds of 8 long requests, each round executes two 800-token requests per GPU. Shared stage: A5000 6 routes/releases, A4000 10 routes/releases, 20 capacity rejections each including retries; peaks 20840448/21430272 bytes. After idle A4000 graceful stop, four further A5000 routes/releases and six capacity rejections; A4000 explicitly rejected with worker_unhealthy and worker_draining. Final independent log audits: 10/10 routes/releases on each GPU, zero outstanding, terminal/quiescent/admission-closed receipts, peak 21430272 bytes each. Worker image `sha256:066466ef743541af12c24af1ce47d3e8a1ced18bc32a7c4318c6b23edf9f44e9`; control `sha256:26e888d703e4981af814c756dc27b3109e2826ec0aec064ea33a8d43f6026698`. Validator stdout and normal `freechat-observed-scheduler` logs, 2026-09-21 local date; registered GPU IDs end in 824cdb4368e9/f5e4470b9bb0. Registration must be confirmed before testing; a prior run that exercised only one Worker failed and is not acceptance evidence. In-memory control/log fallback; no throughput, crash/in-flight recovery, restart-state durability, cross-node, H100 or TP/PP claim. |
| concurrent-kv-admission | VERIFIED | Real concurrent requests cannot over-reserve the measured single-rank KV pool; excess requests receive capacity backpressure and subsequent requests are admitted after confirmed release. | A5000/A4000 tested independently, real Qwen2.5-0.5B BF16, eager, TP=PP=DP=1, max context 1024, actual vLLM `num_gpu_blocks_override=128`, 16-token/196608-byte blocks. Three rounds of 8 concurrent long requests per card; each round admits two requests that each generate 800 tokens. Including readiness/re-admission, each card has 10 unique routes and 10 terminal, quiescent, admission-closed releases; peak reservation 21430272 bytes stays below usable pool 24969216 bytes. A5000/A4000 have 34/50 logged `vram_capacity` rejections including bounded retries during pending confirmation; these counts are timing-dependent, not comparative performance. Worker image `sha256:b3970d87cd772f052c9b7b05670c10a5174365d4864d1dea67c12c9b3ff53c04`, control image `sha256:ce277d623e541d09390375bb1d05e7bba65c5905a6c765f61335839a9db81856`. Stdout `tools.validate_inference_loop --mode concurrent` plus `--mode audit-log` on normal `freechat-pressure-scheduler-a5000/a4000` logs, 2026-09-21 local date. No result directory, shared multi-worker, cross-node, crash-recovery, throughput or H100 claim. |
| same-host-gpu-inference-loop | VERIFIED | Gateway/Scheduler/Worker perform measured registration, authenticated heartbeats, native three-protocol preparation, physical KV admission and confirmed release on a real single GPU. | Qwen2.5-0.5B BF16/eager, TP=PP=DP=1, 1024 context and an actual 128-block pool; A5000/A4000 tested independently on workstation, 2026-09-21 local date. Fork `39b61e225`; Worker image `sha256:f923e060e328ced0492f46d8d0acfe5eb7590750902e2ec33315c959e502fb9d`, control image `sha256:1fc11375e900996670e21124905ed4748557088c5d4430623fb4404f4e4e0651`. Each GPU passes three protocols × JSON/SSE/generated-text disconnect, then three sequential reuse calls. Each has 13 routes and 13 terminal/quiescent/admission-closed releases, three cancel intents and zero unreleased reservations. Peak reserved bytes A5000/A4000: 23986176/23199744, below usable pool 24969216. The 12/17 admission retries exactly match logged capacity rejection; counts are timing-dependent, not performance comparisons. Stdout and normal `freechat-prefix-main-*` / `freechat-prefix-main4000-*` logs; current source auditor replays those logs without result directories. Separate same-host loops with single-node etcd and log fallback; not shared placement, fault/HA, Harness-performance, A6000 or H100 acceptance. |
| gateway-disconnect-gpu-closure | VERIFIED | Generated-text disconnect reaches Scheduler cancellation intent; reservation release requires an identity-matched aborted, quiescent, admission-closed Worker receipt. | Same runtime/model/images and normal logs as `same-host-gpu-inference-loop`: all six disconnects across three protocols/two GPUs close once. The A4000 Responses receipt arrives through a Query already in flight before the cancellation update; its observed aborted state and execution fences, not the RPC verb, establish stop confirmation. Auditor and deterministic reconciliation tests cover this interleaving without changing runtime release rules. A cleanup-deadline regression reproduced unmarked grpc.aio cancellation escaping as HTTP 500; asyncio-owned deadlines inside shielded cleanup now pass the real two-GPU reruns. Four focused CPU cases reproduce the prior failure and pass the fix; this is not RTO, recovery success-rate, exactly-once or performance evidence. |
| scheduler-native-budget-contract | MECHANISM_ONLY | Gateway transports a transient native body instead of a character estimate. Scheduler prepares only eligible workers, accounts each worker's own token budget and geometry, and binds the selected preparation into its atomic reservation. | CPU tests cover per-worker budgets, insufficient capacity, expiry/incarnation/tenant/body fences, failed preparation, locality before prompt disclosure, gRPC propagation, concurrent capacity competition and prompt exclusion from the ledger. This row is CPU-only; real same-host GPU coverage is recorded separately in `same-host-gpu-inference-loop`. No etcd/NATS fault or performance acceptance. |
| native-kv-budget-enforcement | VERIFIED | Managed single-rank Worker checks block-rounded input/output KV demand against the authenticated dispatch budget before admitting GPU work. | A5000/A4000 Qwen2.5-0.5B, image `sha256:fcf40628bf54dd3b60867ede10eaf3622bc055c6041efa7359f3d1632511349c`; all 9 protocol/mode cases per card pass. A separate stdout-only Chinese/emoji probe on all three protocols rejects 0, 589823 and 589825 bytes, then accepts 589824 bytes with the same identity (41 input + 4 output tokens, 16-token blocks) and matching native usage. Normal container logs retained. This proves the Worker check, not ownership of a real Scheduler lease or full deployment. |
| native-token-preparation | VERIFIED | Native Chat Completions, Responses and Anthropic preprocessing supplies request-bound token budgets, checked again against the actual prompt and sampling bound before engine submission. | Qwen2.5-0.5B on A5000/A4000, image `sha256:d36a92a1367749ed41eeb4864892deeb47dfdbf13562190c45b08fe783c44d0e`; all 9 protocol/mode cases per GPU pass and each protocol JSON usage matches its prepared input count. CPU tests additionally cover expiry, tenant/body mismatch, bounded concurrent preparation, native parser mutation and changed engine inputs. Stdout and normal service logs only. Not yet Scheduler admission, multi-model/Harness matrix or performance evidence. |
| engine-kv-geometry | VERIFIED | The managed single-rank Worker reports allocator-derived KV geometry, validates tensor-byte consistency and excludes the null block from usable capacity. | Real Qwen2.5-0.5B on A5000/A4000 through the named vLLM Worker extension RPC; image `sha256:1820964efdc1533f515068b673361787ebf22ce65c3d4f9731834c37a6b6d9fe`. Both use 16-token/196608-byte blocks; 18559 blocks/3648847872 allocated bytes on A5000 and 14181 blocks/2788098048 bytes on A4000. Validation stdout and normal container logs only. These are configured-run gross capacities, not live free space, integrated admission, parallel-rank or performance evidence. |
| native-worker-http | VERIFIED | Managed native Chat Completions, Responses and Anthropic Messages produce JSON/SSE, return fenced execution receipts after normal completion or generated-text cancellation, and reject duplicate/unauthenticated admission. | A5000 and A4000 real Qwen2.5-0.5B, 9 cases per GPU via `tools.validate_native_http`; runtime image ID `sha256:6e841ecbffd92fcaaa5a886116f922197b0c00bcd2382d315308cc9d0d185ae2`. Results go to stdout and container logs. TP=PP=DP=1, synchronous, text n=1 only; no full Gateway/Scheduler, restart, H100, HA or performance claim. |
| single-gpu-execution-adapter | MECHANISM_ONLY | Concrete AsyncLLM admission, completion and cancellation return fenced GPU-quiescent receipts; duplicate admission is rejected. | `evidence/execution-gpu/20260921/`: real Qwen2.5-0.5B weights on A5000, 12 completed calls, post-submit cancellation and cancellation after the first generated token. Fork `7bebebb5abbe5055d220b476bda863d78876b666`. Only synchronous TP=PP=DP=1, text n=1, no KV/EC connector; not HTTP/Gateway/Scheduler integration, restart acceptance, performance or release-image acceptance. |
| worker-durable-admission-contract | MECHANISM_ONLY | A single-owner Worker-local SQLite journal rejects duplicate/late dispatch, persists cancellation and observation numbering, and retains uncertain execution until backend quiescence and submission fencing are observed. | `evidence/worker-admission/20260920/`, `Git da913f9:docs/worker-admission.md`. Local CPU regression, abrupt child-process exit, actual Python child lifetime and loopback RPC/Scheduler integration; not a vLLM backend, authenticated inference ingress, physical GPU abort, HA, exactly-once inference or performance claim. |
| request-execution-confirmation-contract | MECHANISM_ONLY | Gateway completion retains request capacity; the default Scheduler can query/abort through an explicitly configured loopback runtime and atomically release only on an incarnation-bound terminal, quiescent and admission-closed receipt. | `evidence/request-execution/20260920-final/`, `Git da913f9:docs/request-execution.md`. CPU fixture receipts and in-memory reconstruction, not real engine admission fencing, cancellation, physical budget accounting, mTLS or inference exactly-once. |
| group-runtime-reconciliation-contract | MECHANISM_ONLY | Explicit local configuration connects default Scheduler startup to typed group commands, fresh readiness/budget checks and generation/phase-fenced loopback runtime receipts; uncertain termination retains allocation. | `evidence/group-reconciliation/20260920-final/` and `Git da913f9:docs/execution-closure.md`. In-memory stores and a non-serving CPU driver; no model/process deployment, per-request completion, physical budget, mTLS, HA or performance evidence. |
| architecture-contract | VERIFIED | The repository defines versioned Agent hints, capability-first placement and lifecycle-aware KV policy. | Unit and scale tests in this repository. |
| harness-schema-mappings | VERIFIED | Four named Harness mappings can encode the common lifecycle and identity schema into the FreeChat request extension. | `libs/harness-adapters/tests/test_adapters.py`. This verifies schema mapping only, not integration with the real Harness runtimes. |
| openai-agents-contract | VERIFIED | A framework-native OpenAI Agents SDK model decorator and run hooks preserve existing request settings and carry a completed tool call into the next model request as an authenticated Resume hint. | OpenAI Agents SDK 0.22.0, real SDK Runner with scripted model, single/parallel function tools, streaming, duplicate/unknown callback, missing-ID, terminal/cancel/reset tests in `libs/harness-adapters/tests/test_openai_agents.py`. Benchmark acceptance/timeout/tracing configuration tests are in `tests/test_openai_agents_benchmark.py`. This is an integration-contract test, not a Gateway, real-model or performance claim. |
| langgraph-contract | VERIFIED | A LangGraph-native request context preserves explicit task, thread and checkpoint identity across an interrupt and `Command(resume=...)`, and emits paired Tool Wait and Resume hints without relying on unstable runtime internals. | LangGraph 1.2.11, real `StateGraph` plus `InMemorySaver` interrupt/resume test and `libs/harness-adapters/tests/test_langgraph.py`. This is an integration-contract test, not a Gateway, real-model or performance claim. |
| opencode-event-contract | VERIFIED | The OpenCode event bridge maps native session, message, part and tool-call identities to Tool Wait and Resume, keeps parallel calls waiting until all finish, rejects cross-session data and ignores stale state regressions. | Official OpenCode `message.part.updated` ToolPart shape; `libs/harness-adapters/tests/test_opencode.py`. This is a deterministic event-contract test, not a live OpenCode run or performance claim. |
| openhands-event-contract | VERIFIED | The OpenHands event bridge correlates Action and Observation/error pairs by tool-call identity, preserves parallel Tool Wait state, and prevents replayed late actions from reopening completed calls. | Official OpenHands SDK Action/Observation correlation semantics; `libs/harness-adapters/tests/test_openhands.py`. This is a deterministic event-contract test, not a live OpenHands run or performance claim. |
| harness-integrations | UNVERIFIED | OpenAI Agents SDK, LangGraph, OpenCode and OpenHands integrations execute real lifecycle, tool-wait, resume, cancellation and failure paths. | Current 2026-09-21 A5000/A4000 SDK runs verify managed request closure separately above, but both fail strict answer quality (nonzero CLI exit). Tool Wait is still local adapter state rather than an independent control event; real Harness cancellation/failure matrix and KV actions remain pending. LangGraph, OpenCode and OpenHands still require protocol-matched real-Harness runs and trace replay. |
| gateway-streaming-contract | VERIFIED | Streaming proxy setup resolves the worker response before sending client headers, preserves upstream success/error status and media type, relays raw SSE bytes, closes the upstream response, and maps connection failures to a gateway error. | HTTPX streaming transport contract tests in `services/gateway/tests/test_gateway.py`. This is protocol-level test evidence, not a real-model latency or cancellation-performance claim. |
| gateway-lease-lifecycle-contract | MECHANISM_ONLY | Gateway renews during header waits, nonstream responses and streaming; completed responses record pending confirmation, while transport failures and interrupted streams request cancellation. Both retain capacity until execution confirmation. | `evidence/request-execution/20260920-final/`: mocked HTTP transport and real loopback gRPC tests. Current CPU coverage also checks transport cancellation without an AnyIO scope marker and in-flight Query/cancel reconciliation. This evidence is CPU-only; physical completion/cancellation now has the separate `same-host-gpu-inference-loop` and `gateway-disconnect-gpu-closure` entries. Earlier request-admission evidence covered different release semantics. |
| control-images | VERIFIED | Gateway and WebUI images build from locked base-image digests and start as non-root services. | workstation container build and in-container health checks, 2026-08-31. |
| worker-container | BLOCKED | The fork-built CUDA 13 Worker image matches the locked stack and runs vLLM plus FlashInfer on a physical GPU. | The fork image passed the A4000 GPU runtime, CUDA 13, locked-version, revision-label and synchronized FlashInfer sampling checks on 2026-09-07. It remains blocked because the local tag has no immutable repository digest. See `Git da913f9:docs/worker-container.md` and `evidence/worker-images/worker-8e78a3c61307-a4000-local-tag.json`. |
| control-state | UNVERIFIED | The current request reservation ledger and lifecycle intents require real etcd/NATS restart and fault acceptance. | Historical workstation Compose integration on 2026-08-31 covered decision-only leases, not the current request ledger. Its artifacts remain historical evidence; the obsolete decision-only `live_validation.py` CLI has been retired rather than presented as current acceptance. Current local evidence is `evidence/request-admission/20260915/`, not multi-replica HA. |
| engine-hook-contract | VERIFIED | The pinned vLLM fork exposes opt-in allocate, hit, free and eviction callbacks; authenticated tenant/cache identity and generation fences are preserved by the worker bridge without blocking the engine callback thread. | Fork commits `5871f38ee947760ed7f5dd5487bb04456db8bc98` and `8681c8040632e0790c1880c90cddb92420189b07`; focused fork tests, `worker/tests/test_vllm_adapter.py`, and an A4000 serving probe that correlated HTTP lifecycle identity with EngineCore allocate/free/hit events. The serving probe used dummy weights and is not performance evidence. |
| anthropic-real-model-contract | VERIFIED | A real Qwen2.5-0.5B Anthropic Messages request on A4000 preserves authenticated Tool Wait identity through request conversion into EngineCore allocate/free events. | Workstation deployment commit `17539077af862832a2804429e9c0fd8896e274bc`; archived JSONL SHA-256 `5562bd487d3f870fff199efdfc61d5fc0a3db1b3d0e89b257d4ea6ce2b4908b2`. The run disabled FlashInfer sampling because the host compiler is CUDA 11.5. This is correctness/integration evidence, not performance evidence. |
| lifecycle-retention-contract | VERIFIED | The opt-in vLLM policy keeps bounded, high-priority, near-resume blocks behind ordinary LRU eviction candidates and releases protection on resume, terminal state, cancellation, eviction or expiry; internal Chat Completions, Completions, Responses and Anthropic Messages requests preserve authenticated lifecycle metadata through their protocol conversion paths. | Fork commits `5871f38ee947760ed7f5dd5487bb04456db8bc98` and `8681c8040632e0790c1880c90cddb92420189b07`; bounded-capacity, ordering, expiry, resume and protocol-conversion tests. This is contract evidence, not a cache-hit or end-to-end performance claim. |
| lifecycle-retention-mechanism | MECHANISM_ONLY | Under an intentionally constrained 32 MiB cache, the serving policy changed eviction order and preserved the target resume prefix across deterministic pressure. | A4000 dummy-weight probe, 2026-09-01: native policy retained 16 of 930 prompt tokens; lifecycle policy retained 928. Raw baseline/candidate event hashes are recorded in `Git da913f9:docs/environment-validation.md`. This single probe is forbidden in README/resume performance claims and does not replace real-model paired Harness trials. |
| native-kv-offload-mechanism | MECHANISM_ONLY | The deployable fork image moves real-model KV blocks between A4000 HBM and pinned CPU memory through vLLM's native OffloadingConnector. | A 64 MiB HBM cache pressure probe on 2026-09-07 recorded 131,530,752 bytes GPU-to-CPU and 13,369,344 bytes CPU-to-GPU. See `evidence/native-offload/20260907/`. This validates data movement only, not lifecycle-aware policy or performance gain. |
| predictive-kv-offload-control-chain | MECHANISM_ONLY | The lifecycle-aware Scheduler can authorize native KV offload per request, the Gateway prevents client forgery and forwards the token cap, and vLLM performs the resulting pinned-memory transfers. | A real Qwen2.5-0.5B run on A5000 enabled offload for the target and Resume requests, disabled it for eight pressure requests, and recorded 27,918,336 bytes GPU-to-CPU plus 28,114,944 bytes CPU-to-GPU. See `evidence/predictive-offload/20260908/`. This validates policy authority and movement, not prediction quality or performance gain. |
| kv-quantize-historical | HISTORICAL | The isolated fused KV INT8 kernel measured 6.2–7.7x on three GPUs under the superseded PyTorch 2.11/Triton 3.6 evidence stack. | `Git da913f9:docs/kernel-candidates.md`; retained for provenance only and forbidden in current resume material until replicated under the current lock. |
| kv-quantize-current | UNVERIFIED | Fused KV INT8 kernel improvement under the current deployable worker stack. | Requires three-GPU correctness, profiler, microbenchmark and raw artifacts under PyTorch 2.13/Triton 3.7. |
| gpu-performance | UNVERIFIED | GPU or Agent-task performance improvement. | Requires target-system benchmark artifacts. |
| worker-telemetry-ingest | MECHANISM_ONLY | Real vLLM queue, cache usage and transfer-window metrics reach a generation-fenced Scheduler heartbeat; missing prefill calibration disables predictive offload. | A5000 Qwen2.5-0.5B probe `evidence/telemetry/20260908-run03/`, including raw Prometheus snapshots. The single observed Store service-rate window is not a calibrated bandwidth or performance claim. The scheduler in this probe uses an isolated in-memory registry. |
| production-ha | BLOCKED | Production HA validation. | Requires a third physical failure domain. |
| reuse-forecast-edge-contract | VERIFIED | Four adapters no longer infer future reuse probability from lifecycle alone; supplied forecasts are identity/time scoped and Scheduler rechecks them after transport. | CPU tests `libs/harness-adapters/tests/test_forecasts.py` and `services/scheduler/tests/test_forecast_fence.py`; `Git da913f9:docs/reuse-forecasts.md`. This verifies the edge contract, not trained prediction, authenticated evidence references, live provider integration or performance. |
| harness-preoffload-boundary | MECHANISM_ONLY | Two real Harness runtimes demonstrate external KV recovery and reduced resume recomputation under controlled pressure; no-pressure trials expose unused CPU copies. | `evidence/harness-boundary/20260908-paired01/analysis.json`: 80 pairs, A5000 Qwen2.5-0.5B, forced coding workflow. Scheduler/predictor bypassed, strict answer formatting failed, adjusted task-latency intervals cross zero; no resume performance claim or measured A/B superiority. |
| scoped-native-transfer-calibration | MECHANISM_ONLY | Isolated native Store/pressure/Load cycles yield byte-scoped bidirectional transfer observations with correlated external prefix hits. | A5000 Qwen2.5-0.5B; two exact sizes, three measured cycles each; `evidence/transfer-calibration/20260908-run01/` and `Git da913f9:docs/transfer-calibration.md`. Not a live residency catalog, automatic policy calibration or Harness performance claim. |
| scoped-service-calibration | MECHANISM_ONLY | Isolated uncached request observations produce generation/instance-bound service profiles; out-of-scope routes explicitly fall back to least-load. | A5000 Qwen2.5-0.5B, two exact input lengths and three samples each; `evidence/calibration/20260908-run01/` and `Git da913f9:docs/service-calibration.md`. In-memory gRPC probe only; no concurrent, transfer or Harness performance claim. |
| kernel-e2e | UNVERIFIED | Triton or memory-pipeline end-to-end gain. | Requires correctness, profiler, microbenchmark and Agent trace evidence. |
| review-resource-scope-registration-contract | MECHANISM_ONLY | Local request paths enforce explicit per-rank KV feasibility, trusted origin propagation, identity-scoped salts and monotonic CAS-backed registration/heartbeat updates. | `evidence/review-hardening/20260914/`: CPU, in-memory CAS, loopback gRPC and mocked inference endpoint. Request reservations have separate follow-up evidence below; Worker budget reporting, exact tokenizer accounting, authenticated ownership and distributed reconciliation remain incomplete. |
| request-reservation-atomic-intent | MECHANISM_ONLY | The default gRPC path atomically commits bounded request capacity reservations and lifecycle intent; concurrent contenders re-plan, renewal extends actual expiry and duplicate release does not repeat accounting. Expiry and cancellation retain uncertain capacity. | `evidence/request-admission/20260915/`: in-memory CAS faults, local process-object reconstruction, Hypothesis sequences, mock HTTP and loopback gRPC. No real store/engine crash, inference exactly-once, physical capacity, throughput or RTO claim. |
| h100-static-group-local-contract | MECHANISM_ONLY | Atomic static TP=1/2/4 group reservation, lifecycle/owner/generation/engine fences and group-bound routing pass local synthetic 3-node/12-GPU tests. | `evidence/h100-local/20260914/`: in-memory CAS and real loopback gRPC only; no H100, inference, etcd HA, throughput or deployment claim. |
| forecast-grpc-roundtrip-contract | MECHANISM_ONLY | Gateway-to-Scheduler loopback preserves forecast metadata and active resume horizon, and rejects an expired forecast after transport. | `services/scheduler/tests/test_grpc_server.py`; `evidence/h100-local/20260914/`. This repairs the missing transport coverage of the earlier edge contract; it does not validate forecast calibration, provenance authentication or live Harness performance. |
