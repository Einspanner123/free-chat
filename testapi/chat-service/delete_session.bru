meta {
  name: delete_session
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/api/v1/chat/sessions/{{session_id}}
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwt_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
